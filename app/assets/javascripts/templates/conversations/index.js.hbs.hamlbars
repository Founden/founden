.wrapper
  .bucket
    .bucket-title
      Conversations
    %ul.conversations
      {{#each conversation in content}}
      {{#with conversation}}
      {{partial 'conversations/partials/conversation'}}
      {{/with}}
      {{/each}}
